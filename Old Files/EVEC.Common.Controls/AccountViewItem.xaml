<UserControl x:Class="AccountViewItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{Binding Path=Items}">
    <Expander Header="{Binding Path=UserID}" x:Name="pnlAccountDetails" Style="{DynamicResource GlassyBlackExpander}">
        <ListBox x:Name="lstCharacters" ItemsSource="{Binding}" IsEnabled="False" Style="{DynamicResource GlossyBlackListBox}" ItemContainerStyle="{DynamicResource GlossyBlackListBoxItem}" ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Width="auto">
                        <StackPanel Orientation="Vertical" Width="auto">
                            <TextBlock Padding="0,0,5,0" Text="{Binding Path=Character}"/>
                        </StackPanel>

                        <StackPanel Orientation="Vertical" Width="auto">
                            <TextBlock Padding="0,0,3,0" Text="{Binding Path=ActiveString}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Expander>
</UserControl>
